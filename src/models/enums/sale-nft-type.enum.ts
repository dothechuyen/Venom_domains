export enum SaleTypeEnum {
  FIX_PRICE,
  ENGLISH_AUCTION,
}

export enum SaleStatusEnum {
  MINTED = -1,
  LISTING,
  CANCEL,
  SOLD_OUT,
  NOT_FOR_SALE,
}

export enum NftVerifyEnum {
  UNVERIFY,
  VERIFY,
}

export enum OfferSaleStatusEnum {
  ACCEPTED,
  NOT_ACCEPTED,
  CANCEL,
}
